#include "2-3.h" // 2-3.h 파일 포함하기 (파일 안에 stdio.h 있음)

int main(){
    int add_result, sub_result; // 결과값 변수 선언
    printf("10과 5를 더하면 %d이다 \n", ADD(10, 5)); 
    printf("10과 5를 빼면 %d이다 \n", SUB(10, 5));
}